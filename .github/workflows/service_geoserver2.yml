on:
  push:
    branches:
      - main
    paths:
      - Makefile
      - services/geoserver2
      - tomcat
  pull_request:
    branches:
      - main
    paths:
      - Makefile
      - services/geoserver2
      - tomcat

jobs:
  Release:
    uses: ./.github/workflows/ghcr_build_service.yml
    with:
      name: geoserver2
